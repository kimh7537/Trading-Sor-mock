build*/
*.o
*.a
compile_commands.json

# 에디터 · 에이전트 도구 디렉터리
.claude/
.omc/
.serena/

# test_compare가 남기는 임시 출력. 정상 종료 시 스스로 지우지만
# 중단된 실행(변이 검사 등)이 남길 수 있다.
test_compare_out*.md

# Java 빌드 산출물 (channel/)
target/

# 프론트엔드 (web/)
node_modules/
dist/
web/node_modules/
web/dist/
.env

# ctest를 저장소 루트에서 돌리면 남는 임시 출력
Testing/
users.json
users.json.tmp

# 거래 기록 저장소 (T11-02). 남의 가입·체결 기록을 공개 저장소에 올리지 않는다
minisor.db
minisor.db-wal
minisor.db-shm
