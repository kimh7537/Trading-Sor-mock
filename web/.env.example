# 채널계 주소. 비워 두면 같은 출처로 부르고, 개발 서버가 /api·/ws를 채널계(8080)로
# 넘긴다(vite.config.ts). 채널계를 직접 가리키면 채널계에 CORS 설정이 없어 브라우저가
# 주문 POST를 막는다 — 다른 호스트에 채널계를 둘 때만 채운다.
# VITE_API_BASE=http://localhost:8080
# VITE_WS_URL=ws://localhost:8080/ws/stream
